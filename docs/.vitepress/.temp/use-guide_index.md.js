import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Use Guide","description":"","frontmatter":{"layout":"doc","title":"Use Guide"},"headers":[],"relativePath":"use-guide/index.md","filePath":"use-guide/index.md"}');
const _sfc_main = { name: "use-guide/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="use-guide" tabindex="-1">Use Guide <a class="header-anchor" href="#use-guide" aria-label="Permalink to &quot;Use Guide&quot;">​</a></h1><p>This guide explains how to work with BobDyn after the environment is set up.</p><div class="info custom-block"><p class="custom-block-title">Starting point</p><p>In this guide, <code>/BobSim</code> means the root of the <code>../BobSim</code> checkout. Start there before running commands or editing workflow files.</p></div><p>The short version is:</p><ol><li>choose a workflow</li><li>edit its YAML configuration</li><li>run the simulation</li><li>inspect the metrics, plots, and report</li></ol><h2 id="what-bobdyn-is-doing" tabindex="-1">What BobDyn is doing <a class="header-anchor" href="#what-bobdyn-is-doing" aria-label="Permalink to &quot;What BobDyn is doing&quot;">​</a></h2><p>BobDyn keeps the physical model and the simulation workflow separate:</p><ul><li><strong>BobLib</strong> defines the vehicle and its subsystem architecture</li><li><strong>BobSim</strong> runs the standard studies, extracts signals, and produces reports</li><li><strong>BobDocs</strong> explains the workflow, terminology, and outputs</li></ul><p>That separation is what lets you change the study without hiding the physics behind a black box.</p><h2 id="the-main-workflows" tabindex="-1">The main workflows <a class="header-anchor" href="#the-main-workflows" aria-label="Permalink to &quot;The main workflows&quot;">​</a></h2><p>The currently active standard workflows in BobSim are:</p><ul><li><code>SteadyStateEval</code> for steady-state cornering characterization</li><li><code>TransientEval</code> for transient steering response and frequency-response analysis</li></ul><p>KnC, envelope tools, visualization, and DOE are documented as well, but the standard eval workflows are the most reliable place to start.</p><h2 id="steadystateeval" tabindex="-1">SteadyStateEval <a class="header-anchor" href="#steadystateeval" aria-label="Permalink to &quot;SteadyStateEval&quot;">​</a></h2><p>Use <code>SteadyStateEval</code> when you want a quasi-steady characterization of the vehicle across a radius sweep.</p><p>What it does:</p><ul><li>holds vehicle speed constant</li><li>sweeps turn radius in both directions</li><li>runs the unified <code>BobLib.Standards.VehicleSim</code> executable</li><li>averages the settled part of each run</li><li>produces a report and a metrics CSV</li></ul><p>Relevant files:</p><ul><li><code>_3_StandardSim/SteadyStateEval/steady_state_eval_sim.py</code></li><li><code>_3_StandardSim/SteadyStateEval/steady_state_eval_config.yml</code></li></ul><p>Typical output signals include steering, lateral acceleration, roll, sideslip, yaw rate, wheel loads, and handwheel torque.</p><h3 id="transient-response-checks" tabindex="-1">Transient Response Checks <a class="header-anchor" href="#transient-response-checks" aria-label="Permalink to &quot;Transient Response Checks&quot;">​</a></h3><ul><li>steering angle versus lateral acceleration</li><li>curvature or radius tracking error</li><li>roll and sideslip trends</li><li>understeer behavior across the sweep</li></ul><h2 id="transienteval" tabindex="-1">TransientEval <a class="header-anchor" href="#transienteval" aria-label="Permalink to &quot;TransientEval&quot;">​</a></h2><p>Use <code>TransientEval</code> when you want to study response to steering inputs over time.</p><p>What it does:</p><ul><li>generates step and sinusoidal steering cases</li><li>runs left and right directions when configured</li><li>measures time histories directly from the simulation output</li><li>computes gain and phase behavior from the sine cases</li><li>writes a report and a metrics CSV</li></ul><p>Relevant files:</p><ul><li><code>_3_StandardSim/TransientEval/transient_eval_sim.py</code></li><li><code>_3_StandardSim/TransientEval/transient_eval_config.yml</code></li></ul><h3 id="what-to-look-for" tabindex="-1">What to look for <a class="header-anchor" href="#what-to-look-for" aria-label="Permalink to &quot;What to look for&quot;">​</a></h3><ul><li>steering step response</li><li>yaw-rate settling</li><li>lateral acceleration rise time</li><li>phase lag and gain trends versus frequency</li></ul><h2 id="how-to-run-a-study" tabindex="-1">How to run a study <a class="header-anchor" href="#how-to-run-a-study" aria-label="Permalink to &quot;How to run a study&quot;">​</a></h2><p>The most practical way to use BobSim is to edit the YAML config for the workflow you want to run.</p><h3 id="common-config-sections" tabindex="-1">Common config sections <a class="header-anchor" href="#common-config-sections" aria-label="Permalink to &quot;Common config sections&quot;">​</a></h3><table tabindex="0"><thead><tr><th style="${ssrRenderStyle({ "text-align": "left" })}">Section</th><th style="${ssrRenderStyle({ "text-align": "left" })}">Purpose</th></tr></thead><tbody><tr><td style="${ssrRenderStyle({ "text-align": "left" })}"><code>simulation</code></td><td style="${ssrRenderStyle({ "text-align": "left" })}">Build directory, executable name, solver, output format, time settings, and runner flags</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}"><code>execution</code></td><td style="${ssrRenderStyle({ "text-align": "left" })}">Parallelism, worker count, cleanup, and log streaming</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}"><code>sweep</code> / <code>test</code></td><td style="${ssrRenderStyle({ "text-align": "left" })}">Case-generation inputs for the study</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}"><code>report</code></td><td style="${ssrRenderStyle({ "text-align": "left" })}">Title page metadata and report output path</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}"><code>plots</code></td><td style="${ssrRenderStyle({ "text-align": "left" })}">Plot layout and signal mapping</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}"><code>fit</code></td><td style="${ssrRenderStyle({ "text-align": "left" })}">Workflow-specific fitting or curve logic</td></tr></tbody></table><h3 id="typical-run-pattern" tabindex="-1">Typical run pattern <a class="header-anchor" href="#typical-run-pattern" aria-label="Permalink to &quot;Typical run pattern&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">omc</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> _3_StandardSim/build.mos</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> SteadyStateEval</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> TransientEval</span></span></code></pre></div><p>If you want to run only one workflow, just run the corresponding target or module.</p><h2 id="where-results-go" tabindex="-1">Where results go <a class="header-anchor" href="#where-results-go" aria-label="Permalink to &quot;Where results go&quot;">​</a></h2><p>BobSim writes the active standard-study outputs under:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>_3_StandardSim/results/</span></span></code></pre></div><p>That directory usually contains:</p><ul><li>a PDF report for human review</li><li>a metrics CSV for downstream analysis</li><li>per-case run folders while a job is executing</li></ul><p>The per-run folders are useful when debugging a failed case because they contain the overrides file, log output, and result CSV.</p><h2 id="reading-the-outputs" tabindex="-1">Reading the outputs <a class="header-anchor" href="#reading-the-outputs" aria-label="Permalink to &quot;Reading the outputs&quot;">​</a></h2><p>Use the report first if you want a quick engineering readout.</p><p>Use the metrics CSV if you want:</p><ul><li>a spreadsheet-friendly summary</li><li>comparison across runs</li><li>post-processing in Python, MATLAB, or a notebook</li></ul><p>Use the raw case outputs if you need:</p><ul><li>a specific time history</li><li>a failed-run log</li><li>deeper investigation of a suspicious metric</li></ul><h2 id="related-documentation" tabindex="-1">Related documentation <a class="header-anchor" href="#related-documentation" aria-label="Permalink to &quot;Related documentation&quot;">​</a></h2><p>If you want to dig deeper, these pages are the next stop:</p><ul><li><a href="/bobsim/">BobSim overview</a></li><li><a href="/boblib/">BobLib overview</a></li><li><a href="/reference/">Reference index</a></li><li><a href="/reference/metrics">Vehicle performance metrics</a></li><li><a href="/reference/control-theory">Control theory</a></li></ul><h2 id="practical-advice" tabindex="-1">Practical advice <a class="header-anchor" href="#practical-advice" aria-label="Permalink to &quot;Practical advice&quot;">​</a></h2><ul><li>Start with <code>SteadyStateEval</code> if you are validating a new setup.</li><li>Use <code>TransientEval</code> when you care about response dynamics instead of a settled operating point.</li><li>Keep the report and metrics CSV under version control only when you intentionally want to show a baseline result.</li><li>When a run fails, check the run log before changing the model. Many issues are configuration or build-path problems, not physics problems.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("use-guide/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
